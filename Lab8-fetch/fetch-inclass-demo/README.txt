For the server application setup:
1. Navigate your folder into server package:
2. run command: npm install  - this command is to install 3rd party dependencies
3. run node app.js  to start the application.

For the client side: just open the HTML in browser.